# create_instance.sh
Bash script to automate the creation of an AWS EC2 instance using the AWS CLI. It checks for AWS CLI installation, installs it if missing, and launches an EC2 instance with specified parameters.
